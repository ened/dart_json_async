name: json_async
description: Decode JSON asynchronously, in a long-running Isolate.
version: 0.0.2
homepage: https://github.com/ened/dart_json_async

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  semaphore: ^0.1.1

dev_dependencies:
  test: ^1.6.1
